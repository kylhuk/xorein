# Phase 4 `F14` Acceptance Matrix

| Requirement | Metric | Evidence |
|-------------|--------|----------|
| Voice session join | Transition to `in-call` when signaling completes | Pending v14 runtime tests |
| Fallback routing | Relay used when direct fails | Documented routing matrix |
| Recovery | Explicit `recover` action resets `pending` | Spec-level sequence diagrams |
