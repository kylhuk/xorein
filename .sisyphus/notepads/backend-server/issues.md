- 2026-04-21: The new `pkg/network` seam is a bootstrap boundary only for now; full protocol-native negotiation/peer handlers still need Task 2, so the legacy HTTP adapter remains as a non-production fallback inside `pkg/node.Service` for existing tests and transitional orchestration.
- 2026-04-21: Initial review flagged `legacyPeerRuntime` as a hidden second runtime path; fixed by removing the implicit fallback from `Service.Start()` and updating node tests to inject the adapter explicitly.
- 2026-04-21: The current checkout does not reproduce the earlier summary failure; `pkg/node` tests now pass, so the remaining risk is only keeping the Task 1 runtime seam explicit and avoiding any future fallback reintroduction.
- 2026-04-21: `pkg/node/service.go` still contains the old HTTP peer mux/helper methods as unused compatibility shims after Task 2; production traffic no longer uses them, but they should be removed once downstream transport-facing tasks no longer need the transitional code nearby.
- 2026-04-21: The storage seam now encrypts state in-tree, but the SQLCipher research decision is still not fully realized; a later follow-up should swap the temporary key-file/env source over to the eventual SQLCipher-backed implementation without changing the node-facing `pkg/node/state.go` facade.
- 2026-04-21: Task 4 briefly deadlocked in `peerExchange` because code running under `Service.mu` re-entered `PeerID()`; fixed by caching `selfPeerID` before the locked peer traversal so discovery/peer exchange keeps its behavior without recursive locking.
- 2026-04-21: Server membership propagation is still coarse-grained: joined peers do not learn every other member through an explicit membership sync, so Task 5 validation stayed focused on invite/manifest consistency and known sender-key binding instead of enforcing full channel membership checks on every recipient.
- 2026-04-21: Relay storage still carries the current signed `Delivery` envelope end-to-end; the runtime now keeps relays opaque by not applying queued payloads locally, but true ciphertext-only payload content still depends on a later encrypted delivery format rather than this Task 6 runtime-only change.
- 2026-04-21: Live `/v1/events` smoke verification over the Unix control socket uses HTTP chunked transfer framing before the SSE payload; future operator smoke tooling needs to parse chunks (or use a client that does) so chunk metadata is not mistaken for an API regression.
- 2026-04-21: `pre-commit run --all-files` still fails in this checkout because the repo is on `main` and the configured `no-commit-to-branch` hook blocks that branch; the gate is working, but local verification on `main` stops at the intended policy check.
- 2026-04-21: `make scan` now runs real host-native scanners, but `govulncheck` reports current Go 1.24.4 standard-library vulnerabilities (`crypto/tls`, `crypto/x509`, `net/url`, `net/http`, `os/exec`, etc.), so the security gate currently fails for a toolchain reason rather than a repo regression.
- 2026-04-21: `cmd/aether/main.go` merges file config behind fully-populated flag defaults, so `--config` values for `data_dir`, `listen_addr`, `control_endpoint`, `history_limit`, etc. are shadowed unless the matching CLI flag is also changed; this makes the JSON config path effectively unusable in the current merge order.
- 2026-04-21: `TestBuildNodeConfigMergesFileConfig` is too narrow to catch the config-shadowing bug because it only asserts role/override behavior and never verifies that file-sourced startup fields survive when their CLI flags are left at defaults.
- 2026-04-21: Live SSE smoke over the Unix socket uses `curl --max-time` to observe the `ready` event; that client exits with timeout code 28 after receiving the initial frame, so QA scripts need to treat that as expected behavior rather than a failure.
- 2026-04-21: F3 live manual QA on the current checkout passed the runtime/control/persistence/peer smoke checks, but the invalid-startup guard is broken: `bin/aether` and a fresh `go build ./cmd/aether` both booted successfully on `--mode definitely-not-a-mode` instead of failing fast with `invalid --mode`, so startup validation is not enforcing the documented mode set.
- 2026-04-21: The same manual QA pass verified the local control plane over the Unix socket (`/v1/state`, `/v1/events`, bearer-token auth), restart recovery for identity/server/DM/message state, and manual-peer transport smoke across two live nodes; those paths were healthy on the fresh binary.

- 2026-04-21: Final compliance audit found two plan mismatches that block approval: peer transport is still implemented as HTTP/JSON over `pkg/network` instead of the planned protocol-native p2p/protobuf wire, and the new storage backend is a custom AES-GCM `state.store/` format rather than the planned SQLCipher-backed store. Relay handling also still persists full `Delivery` envelopes and enforces only bounded queues, not the full ciphertext-only + TTL policy.
- 2026-04-21: Final compliance audit rejected the plan because peer transport still runs as HTTP/JSON over `/_aether/p2p` instead of a protocol-native p2p wire, storage uses a custom AES-GCM `state.store/` layout instead of the planned SQLCipher-backed store, and relay queues still retain full `Delivery` envelopes without the planned ciphertext-only + TTL policy.
- 2026-04-21: Scope fidelity review found no unrelated product work; the remaining churn is backend/runtime code plus alignment-only docs and generated verification artifacts, which should stay treated as non-source.
- 2026-04-21: The smoke harness had to fall back from `python` to `python3` in this environment; that was only a local tooling quirk, not a backend failure.
- 2026-04-21: `cmd/aether/main.go` still merges file config under live CLI defaults, so `--config` cannot reliably set `data_dir`, `listen_addr`, `mode`, or `history_limit` unless the matching flags are also changed; this makes the JSON config path shadowed for core startup fields.
- 2026-04-21: `pkg/storage.Save()` activates the new generation before `meta.json` is rewritten, and `openMetadata(create=true)` writes a metadata file with an empty `CurrentGeneration` first; a crash or metadata-write failure in that window can strand the latest generation or make a fresh store look corrupt on restart.
- 2026-04-21: The activation/restart regression is fixed by reading/writing against the newest generation directory and creating an initial generation on store creation, which prevents stale or empty metadata from stranding a valid snapshot after a crash window.
- 2026-04-21: Review gap: `cmd/aether/main_test.go` no longer exercises a conflicting file-vs-flag case; the new config test proves file defaults survive, but it does not lock in the intended precedence when a file value and an explicit CLI override disagree.
- 2026-04-21: Live QA did not surface any new user-facing defects after the config/storage fixes; the only expected failures were auth rejections (`401`) for wrong or missing bearer tokens on the Unix control socket.
- 2026-04-21: Re-run F4 scope audit after config/storage fixes: backend/runtime, protocol negotiation, storage, discovery, control-plane, and verification-gate changes dominate; the remaining docs/meta edits (AGENTS.md, README.md, docs/v0.1/*, pkg/phase8/UI_DESIGN_PRD.md) are cosmetic/alignment churn, and generated artifacts/bin outputs should stay excluded from source-scope judgments.
- 2026-04-21: Re-audit after the config/storage fixes still finds the same plan mismatches: peer transport is HTTP/JSON over `/_aether/p2p` instead of a protocol-native p2p wire, and `pkg/storage` remains a custom AES-GCM `state.store/` backend rather than the SQLCipher-backed store named in the plan.
- 2026-04-21: Re-run F1 still REJECTS approval; the config/storage fixes are sound, but plan scope remains mismatched because `pkg/network` is still HTTP/JSON over `/_aether/p2p` and `pkg/storage` is still the custom AES-GCM `state.store/` backend instead of the plan's protocol-native p2p + SQLCipher path.
 - 2026-04-21: Final context pass verdict is FAIL: storage/control-plane are now aligned with the SQLCipher + local-only boundary decisions, but transport still falls short of the plan because `pkg/network` uses direct TCP + length-delimited protowire envelopes with JSON payload marshalling rather than the plan/README's protocol-native libp2p/p2p wire, and relay storage still queues full `Delivery` envelopes instead of the planned ciphertext-only/TTL behavior.
 - 2026-04-21: Evidence checked for that verdict included `.sisyphus/plans/backend-server.md`, `docs/local-control-api-v1.md`, `docs/v0.1/phase2/p2-t10-sqlcipher-decision.md`, `README.md`, `pkg/node/control.go` blame, `pkg/network/runtime.go`, `pkg/network/transport.go`, `pkg/storage/store.go`, `pkg/storage/legacy_store.go`, and `pkg/node/service.go` relay paths.
- 2026-04-21: The local LSP in this environment continued to report stale import/undefined errors for `pkg/storage/store.go` after the SQLite dependency landed, but repeated `go test ./pkg/storage ./pkg/network ./pkg/node`, `go test ./...`, and `go build ./...` all passed; the compiled/tested repo state is healthy despite that tooling cache issue.
- 2026-04-21: The local LSP still reports stale unresolved-import errors for `github.com/mutecomm/go-sqlcipher/v4` in `pkg/storage/store.go` even after `go get`, `go mod tidy`, focused tests, the full `go test ./...` suite, and `go build ./...` all passed; this remains an editor/tooling cache issue rather than a repository build failure.
- 2026-04-21: Final F1 plan audit still blocks approval on transport scope: `pkg/network/runtime.go` is a raw TCP listener and `pkg/network/transport.go` still wraps peer operations in JSON request envelopes (`json.RawMessage`) over length-delimited frames, so the peer runtime has not fully reached the plan's protocol-native p2p/protobuf wire end-state even though storage/config/recovery are much closer.
- 2026-04-21: F2 code quality review result = FAIL.
- 2026-04-21: MAJOR — `pkg/node/service.go:2337-2398` and related peer mutation handlers ignore `s.saveLocked()` failures (`bootstrapRegisterPeer`, `peerJoin`, `peerRelayStore`, `peerRelayDrain`), so join/bootstrap/relay operations can report success even when the durable store never commits. That is a silent persistence failure on core backend paths and should be surfaced or retried explicitly.
- 2026-04-21: MINOR — `cmd/aether/main.go:88-160` still cannot distinguish an explicit CLI flag equal to the default from an omitted flag, so a file config cannot be overridden back to the default literal (for example `--mode client` against a file mode). The current tests don’t lock in that precedence edge case.
- 2026-04-21: QA harness note: the restart step was launched from a fresh shell invocation, so `wait 4015663` reported “not a child of this shell”; the runtime restart itself succeeded, but future live scripts should not depend on shell job-control state persisting across tool calls.
- 2026-04-21: MINOR — `loadState()` silently falls back to creating a fresh state when legacy-backup recovery returns an error other than not-found, which can hide unreadable/corrupt backup data behind a brand-new identity instead of forcing explicit operator attention.
- 2026-04-21: HIGH - `pkg/node.Service.peerRelayDrain()` is not bound to the requesting peer's identity. Any remote client that can complete transport negotiation can call `peer.relay.drain` with an arbitrary `peer_id`, receive queued deliveries, and clear that queue; `drainRelays()` only proves the client sends its own ID, but the server never checks that the caller owns the drained queue, so store-and-forward traffic can be exfiltrated or deleted by an unauthenticated peer.
- 2026-04-21: Backend blocker fix landed and verified: peer transport now carries protobuf `google.protobuf.Value` payload bytes instead of JSON raw-message envelopes, startup rejects invalid `--mode` and invalid config-file modes deterministically, peer mutation handlers now roll back and surface save failures, and relay drain is signed/bound to the requester peer identity.
- 2026-04-21: Verification passed after the fixes: `go test ./cmd/aether ./pkg/network ./pkg/node ./pkg/storage ./pkg/protocol`, `go test ./...`, and `lsp_diagnostics` on `cmd/aether`, `pkg/network`, `pkg/node`, `pkg/storage`, and `pkg/protocol` all came back clean.
- 2026-04-21: F1 plan audit result = FAIL. The checkout now passes `go test ./...` and `go test -race ./...`, but the plan still is not fully satisfied because peer transport remains a raw TCP/length-delimited protobuf envelope implementation in `pkg/network` rather than the plan’s protocol-native p2p/libp2p end-state, and relay storage still persists full `Delivery` envelopes without the stated ciphertext-only/TTL policy.
- 2026-04-21: F4 deep scope fidelity review verdict = FAIL. The checkout stays inside backend/local-control/verification scope overall, but it still misses the plan end-state in two places: (1) `pkg/network/runtime.go` is a raw TCP listener and `pkg/network/transport.go` hand-encodes operations plus JSON-shaped payloads into `google.protobuf.Value` wrappers instead of using the repos protocol-native p2p/libp2p-style transport and `proto/aether.proto` message types named by the plan/README; (2) `pkg/node/service.go` relay storage still persists full `Delivery` envelopes (including `Body`/`Data`) with a bounded queue only, while the plan/README call for ciphertext-only relay behavior with TTL/quota semantics.
- 2026-04-21: Current review verdict remains FAIL for the same end-state reasons: the new backend/runtime stack still terminates peer traffic on a direct TCP listener with structpb/base64 envelopes in `pkg/network`, so it approximates protocol negotiation but does not reach the plan's protocol-native p2p/libp2p target; relay/history also still stores and drains full `Delivery` envelopes rather than an explicit ciphertext-only + TTL policy.

- 2026-04-21: F2 code quality review result = REJECT.
- 2026-04-21: MAJOR — `pkg/node/service.go:1646-1654` and `pkg/node/service.go:1702-1712`: discovery paths still ignore `saveLocked()` failures after peer-cache updates, so bootstrap/peer-info discovery can appear successful while the persisted known-peer cache never lands.
- 2026-04-21: MAJOR — `pkg/node/state.go:108-123`: if legacy-backup recovery fails for any reason other than `not found`, `loadState()` silently falls back to a brand-new state instead of surfacing the recovery problem, which can hide unreadable backup data and lose state without operator visibility.
- 2026-04-21: MINOR — `cmd/aether/main.go:124-163`: explicit CLI flags that equal the default literal still cannot override file config back to that default, because presence and value are conflated during merge.

- 2026-04-21: HIGH — `pkg/node/service.go:2463-2526`: relay storage queues persist and drain full `Delivery` envelopes, including plaintext `Body`/`Data`, so a relay operator or relay-store compromise can read message contents instead of opaque ciphertext-only payloads.
- 2026-04-21: HIGH — `pkg/node/service.go:1702-1761,2593-2607` and `pkg/network/transport.go:121-123`: discovery accepts and later dials peer-supplied addresses without any loopback/private/internal-network filtering, which lets a malicious peer steer outbound connections toward arbitrary internal targets (SSRF / port-scan exposure).
- 2026-04-21: F5 final context mining verdict = REJECT. The plan/README still require a protocol-native p2p stack and ciphertext-only relay semantics, but the current implementation uses a custom TCP listener plus length-delimited request/response frames in `pkg/network/runtime.go` and `pkg/network/transport.go`, and relay storage still persists full `Delivery` envelopes with only a bounded queue in `pkg/node/service.go:2463-2526,3067-3099` instead of a ciphertext-only + TTL model. Discovery also still ignores `saveLocked()` errors in `fetchPeersFromBootstraps()` and `pingKnownPeers()`, so some learned peers may not persist even though the operations return success.
- 2026-04-21: Follow-up address hardening landed: `network.NormalizePeerAddress()` now feeds `Service` ingest/load/dial paths, so peer caches only persist normalized loopback/IP targets and bootstrap/relay/manual dialing skips unsafe peer-supplied addresses; `go test ./cmd/aether ./pkg/network ./pkg/node ./pkg/protocol`, `go test ./...`, and `go test -race ./...` all passed after the change.
- 2026-04-21: Transport seam follow-up landed: `pkg/network/transport.go` now serializes peer requests/responses as deterministic protobuf `structpb.Struct` envelopes with base64 payload fields instead of hand-built protowire tags, and `pkg/network/runtime_test.go`/`pkg/protocol/transport_test.go` pin the canonical peer protocol/capability surface and the new protobuf wire behavior.
- 2026-04-21: Verification note for the transport follow-up: `go test ./cmd/aether ./pkg/network ./pkg/protocol` passed, but `go test ./...` and `go test -race ./...` both failed in `pkg/node` at `TestNotificationSummaryBucketsExposeLatestMessageIDs` with changing latest-message IDs, which appears unrelated to the transport seam changes.
- 2026-04-21: F2 follow-up review found a new MAJOR risk: `pkg/node/service.go` still updates live state before calling `saveLocked()` in several user-facing paths (`CreateServer`, `CreateDM`, `sendMessage`, `EditMessage`, `DeleteMessage`, `MarkNotificationsReadScoped`, identity rebinding), so those calls can fail while leaving memory ahead of disk and bypassing the rollback pattern now used by discovery/join paths.
- 2026-04-21: The remaining `go test -race ./...` blocker in `pkg/node` was mostly test harness cost, not a new backend race: the notification-summary tests were starting full libp2p-backed bootstrap/host/guest services just to verify local unread aggregation and control-plane JSON reads, so the race suite was burning most of its time in unnecessary runtime startup and peer-join plumbing.
- 2026-04-21: Fixed that blocker in `pkg/node/service_test.go` by moving the summary-focused tests onto a control-only stub runtime plus signed synthetic `applyDelivery()` messages. That kept the meaningful assertions (direct/server/kind aggregates, latest message IDs, participant IDs, labels, and mark-read behavior) while removing the expensive transport dependency; the required focused summary run and the full `go test -race ./...` suite both passed afterward.
