# F24 acceptance matrix (Phase4 P4-T2 ST2)

This matrix maps each `F24` seed to the measurable acceptance checks that keep `G8`, `G9`, `G10`, and `G11` honest. All entries remain planning-only; no implementation claims are made.

| Seed | Acceptance checks | Gate mapping | Evidence notes |
| --- | --- | --- | --- |
| `F24-A` Distributed encrypted blob store | 1. Document ciphertext-only storage rules, signed hash verification, and per-space quotas for attachments/avatars/emojis. 2. Show how storage tiers mirror the audit’s `attachments/blobs` and `custom emojis/stickers`. 3. Confirm relays remain stateless by design. | `G8` (relay regression checks), `G10` (seed catalog), `G11` (audit closure) | Spec doc plus audit line item referencing the same data classes; tie to `EV-v23-G10-###` and `EV-v23-G11-###` when the spec is published. |
| `F24-B` Multi-device history policy guidance | 1. Define per-device backfill limits, cache validity windows, and operator-level reclamation workflows. 2. Include a plan for coverage labels so `G9` can compare actual behavior against the v22 as-built report. 3. Describe how invalidation occurs when quotas or coverage labels change. | `G8` (relay boundary), `G9` (as-built conformance), `G10` | Device/caching spec plus audit cross-check entries for history indexes; feed evidence when the spec is published. |
| `F24-C` Assisted search research pack (opt-in) | 1. Enumerate the hint ledger schema, leakage budget measurements, and opt-in guardrails. 2. Demonstrate how assisted-search metadata is only persisted after explicit consent and is tied to audit hooks so its persistence state is never unknown. | `G10` (seed catalog), `G11` (audit), `G9` (to the extent assisted search references coverage claims) | Research pack and audit table note referencing `assisted search hints`; link to new seed doc for gate review. |
| `F24-D` Desktop background mode daemon | 1. Describe the daemon’s persistent state (OS service metadata, refresh tokens). 2. Show encryption, pruning, and operator supervision so relays/operators can verify no extra persistence sneaks in. 3. If deferred, the deferral register must include the rationale and revisit target (see `f24-deferral-register.md`). | `G8` (relay regression), `G10`, `G11` (if the daemon adds persistence) | Spec + potential deferral note; state that acceptance will be re-verified once the daemon is implemented or explicitly deferred per the deferral register. |
