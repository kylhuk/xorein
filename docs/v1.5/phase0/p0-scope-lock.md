# Phase 0 Scope Lock

- v1.5 scope limited to deterministic capture + transport + UX + validation + spec handoff for v16.
- Deferred v16 implementation; only spec artifacts delivered in this version.
