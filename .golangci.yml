run:
  concurrency: 2
  timeout: 5m
  issues-exit-code: 1
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused
linters-settings:
  govet:
    check-shadowing: true
issues:
  exclude-use-default: false
