# v2.2 Podman History Scenarios

Assets in this folder describe the deterministic scenario probes executed by `scripts/v22-history-scenarios.sh`. The script reads `scenarios.conf`, which lists each probe slug, label, Go package, and test pattern so the harness can run Podman-backed verification without additional manual steps.
