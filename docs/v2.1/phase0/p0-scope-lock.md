# Phase 0 Scope Lock (v2.1)

This artifact formally documents the v2.1 scope freeze for F21 persistence, timeline hydration, and the local-search boundary. Every in-scope change, explicit non-goal, and deferred feature must be recorded here before downstream engineers assume implementation work has officially begun.

The traceability chain in this file links the entry criteria, gating dependencies, and the `G0` assurance artifacts so reviewers can confirm that the scope lock is captured once and referenced everywhere else.

## Evidence cross-reference
- Gate-level artifacts such as `EV-v21-G0-001` are catalogued in the phase 5 evidence index (`docs/v2.1/phase5/p5-evidence-index.md`) to keep the planning-phase verification trail intact.
