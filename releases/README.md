# Release artifacts

The `releases/` directory holds deterministic manifest and handoff documents referenced from `TODO_v10.md`.

- `VA-B1-release-manifest.md` records the release bundle checklist (VA-B* + VA-R* anchors).
- `VA-H1-handoff-dossier.md` compiles go/no-go evidence and open-decision status for handoff.
- `v1.0-checksums.txt` captures SHA-256 outputs for locally built v1.0 binaries under `artifacts/generated/release-pack/v1.0/`.
