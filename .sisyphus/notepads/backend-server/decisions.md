- 2026-04-21: Chose a single negotiated peer transport family (`FamilyPeer`) instead of a new protocol family per peer endpoint, because the task needed the serving surface moved onto the registry/capability layer with minimal wire churn and explicit downgrade handling.
- 2026-04-21: Kept transport capabilities separate from `DefaultFeatureFlags()` so server manifests and existing role-advertisement semantics would not change during Task 2; peer transport negotiation now uses `DefaultPeerTransportFeatureFlags()` only inside `pkg/network`.
- 2026-04-21: Implemented the new storage boundary as encrypted per-bucket snapshot generations under `pkg/storage` instead of introducing SQLCipher dependencies mid-task, because the repo had no existing SQLCipher package or driver wiring and the task explicitly required creating the seam before migration logic.
- 2026-04-21: Used an operator-provided `XOREIN_STATE_KEY` when available and otherwise a generated local `state.key` file as the temporary key source so wrong-key handling is deterministic today while keeping the encryption boundary isolated for a later SQLCipher-backed replacement.
- 2026-04-21: Added a single `peer.exchange` transport operation instead of separate DHT/rendezvous wire methods, because Task 4 needed the new runtime to preserve the discovery order with minimal protocol churn while still letting the service bias discovery by layer-specific request inputs.
- 2026-04-21: Kept backoff state ephemeral and scoped to discovery layer + address rather than persisting retry counters, because the acceptance criteria required known peers to survive retries/restarts, not failed-attempt bookkeeping, and the durable peer cache in `KnownPeers` already provides the restart resilience layer.
- 2026-04-21: Removed the leftover peer HTTP compatibility handlers for manifest/preview/join/deliver instead of keeping a second serving surface, because Task 5 makes the negotiated peer transport the only production path for those peer-facing flows.
- 2026-04-21: Tightened invite validation by requiring owner peer ID, owner public key, server addresses, bootstrap addresses, relay addresses, and manifest hash to agree between the signed invite and fetched manifest, because a valid signature alone was not enough to prove the invite still described the same advertised server state.
- 2026-04-21: Added delivery sender public-key consistency checks against the local identity/known-peer cache before applying remote deliveries, because signed payloads must also stay bound to the claimed sender peer ID once that binding is already known locally.
- 2026-04-21: Bounded each persisted relay queue to a fixed per-recipient window (`relayQueueLimit`) with operation-key dedupe, because Task 6 required store-and-forward semantics to stay safe under restart/offline scenarios without introducing new config surface area mid-plan.
- 2026-04-21: Refreshed owned manifest history fields during startup binding repair instead of only at server creation time, because the history window is a runtime-owned local retention policy and its advertised labels must stay consistent after restarts or config changes.
