# go-fast mode rules

- Implement the requested change only; avoid refactors unless necessary to complete the task.
- Keep edits limited to non-test Go files (+ go.mod/go.sum if needed).
- Hand off immediately for review by `proj-lead`.
