- 2026-04-21: Reworked `cmd/aether/main.go` to boot a `pkg/network` runtime seam instead of constructing the peer-serving path directly from `pkg/node.Service`.
- 2026-04-21: Kept the local control plane in `pkg/node/control.go`; the loopback/socket-only transport and bearer-token auth boundary remained unchanged while startup moved behind the new runtime seam.
- 2026-04-21: `pkg/node.Service` now accepts an injected peer runtime and uses a legacy internal adapter only as a fallback, which preserves current package-level node tests while changing the production bootstrap path.
- 2026-04-21: Tightened the seam after review feedback: `pkg/node.Service.Start()` now requires an injected peer runtime, so only `cmd/aether/main.go` owns the production runtime wiring and the legacy HTTP adapter is test-only by explicit package-local injection.
- 2026-04-21: Re-verified `TestNotificationSummaryBucketsExposeLatestMessageIDs` and adjacent Task 1 startup/runtime checks; the summary code already exposes latest message IDs for buckets, server aggregates, and direct aggregates while `Service.Start()` still rejects missing runtime injection.
- 2026-04-21: Added a canonical peer transport protocol family in `pkg/protocol` (`/aether/peer/0.1.0`) plus transport-specific capability negotiation, so peer runtime requests now fail closed on unsupported protocol versions or required capabilities before any handler logic runs.
- 2026-04-21: Moved peer discovery/join/manifest/delivery/relay calls in `pkg/node/service.go` onto `pkg/network.Client` and `network.P2PRuntime.HandlePeerOperation`, which removes live reliance on ad hoc `/_xorein/*` peer routes while keeping the local control plane untouched.
- 2026-04-21: `proto/aether.proto` was intentionally left unchanged for Task 2 because the transport negotiation contract stayed internal to the Go runtime seam; that avoided unnecessary wire-schema churn and meant Buf proto checks were not required for this step.
- 2026-04-21: Added the missing `pkg/storage` persistence seam and moved `pkg/node/state.go` onto an encrypted generation-based store (`state.store/`) so runtime state now survives restart without keeping `state.json` as the live source of truth.
- 2026-04-21: Legacy `state.json` loads now migrate deterministically into `state.store/`, preserve a `state.migrated-*.json` backup before cutover, quarantine corrupt stores as `state.store.corrupt-*`, and fail closed on wrong-key opens instead of silently regenerating state.
- 2026-04-21: Task 4 rewired `pkg/node.Service` discovery into the planned cascade order: known-peer cache, local in-process LAN discovery, bootstrap register/list, graph walking, rendezvous-biased peer exchange, generic peer exchange, then manual peers.
- 2026-04-21: Discovery retries now fail soft per layer/address with exponential backoff in memory, while persisted `KnownPeers` state is left intact so offline bootstrap nodes do not destabilize the runtime or erase restart state.
- 2026-04-21: Task 5 now rejects invites whose signed owner identity or advertised address sets no longer match the fetched manifest, so preview/join only accept manifest/invite pairs that still describe the same server over the negotiated peer transport.
- 2026-04-21: The remaining `/_xorein/*` manifest/preview/join/deliver compatibility handlers were removed from `pkg/node/service.go`; production and tests now exercise those peer-facing flows only through `network.P2PRuntime` and `network.Client`.
- 2026-04-21: Added focused transport regression coverage for a signed-but-owner-mismatched invite and a tampered delivery signature, alongside the existing forged edit/delete checks.
- 2026-04-21: Task 6 tightened relay storage in `pkg/node/service.go` so only relay-capable roles (`relay` and `bootstrap`) accept store/drain operations; relay queues now dedupe by delivery operation and keep only the newest bounded window per recipient instead of growing forever.
- 2026-04-21: Owned-server manifest refresh now rewrites `HistoryRetentionMessages`, `HistoryCoverage`, and `HistoryDurability` on startup, which keeps restart-time history pruning aligned with the advertised `local-window` / `single-node` semantics.
- 2026-04-21: Task 7 now rebinding local identity changes inside `pkg/node.Service` updates self-owned server ownership/membership, DM participants, voice participants, invites/manifests, and the self peer cache before saving, so `/v1/state` stays aligned with the live runtime after `POST /v1/identities` and `POST /v1/identities/restore` without requiring a restart.
- 2026-04-21: The local control wrapper in `pkg/node/control.go` now treats Unix-socket requests as local by transport and requires loopback IPs for non-socket requests, which makes the documented `/v1` local-only boundary explicit in both the handler path and regression tests.
- 2026-04-21: Task 8 realigned the repo gates to host-native commands: `Makefile` now drives `buf lint`/`buf breaking`, `go build`, `go test`, `go test -race`, `pre-commit run --all-files`, and host-wrapped `govulncheck`/`gosec`/`trivy` through repo-local tool shims under `.cache/xorein-tools`.
- 2026-04-21: `scripts/release-pack-verify.sh` now validates the built `bin/aether` binary with `--preflight` before signing, and the release bundle is assembled on-host with `go run` instead of the old Podman signing scaffold.
- 2026-04-21: Focused transport/storage/node tests and the full `go test ./...` suite passed on this checkout, so the remaining review concern was behavioral quality rather than a failing baseline.
- 2026-04-21: Final manual QA passed on the live runtime: `make build` succeeded, `bin/aether` started with a Unix control socket, `/v1/state` returned the expected client snapshot, wrong bearer tokens returned `401 unauthorized`, and `/v1/events` emitted the expected `ready` SSE frame.
- 2026-04-21: Repo sweep did not find any remaining user-facing bypass paths matching `legacyPeerRuntime`, `/_xorein/`, or the old peer mux labels; the new runtime seam is the only production bootstrap path observed in this checkout.
- 2026-04-21: Live manual QA on the Unix control socket confirmed `/v1/state` returns the live `control_endpoint` and `listen_addresses`, wrong or missing bearer tokens both return `401`, and `/v1/events` emits the initial `ready` SSE frame with `text/event-stream` content type.
- 2026-04-21: `buildNodeConfig()` now treats CLI defaults as non-overrides, so JSON file values win unless the corresponding flag actually differs from the default; this keeps config-file startup usable without extra flag churn.
- 2026-04-21: `pkg/storage` now resolves the newest committed generation on both read and write, so a crash after activation but before metadata rewrite recovers the latest valid snapshot instead of flagging an empty `current_generation` as corruption.
- 2026-04-21: `pkg/node.Service` persists state through `saveLocked()` under `Service.mu`, so the generation swap in `pkg/storage.Save()` still runs with a single writer and does not add a new write race.
- 2026-04-21: Re-ran the live QA gate on a JSON-configured runtime: explicit `data_dir`, `listen_addr`, and `control_endpoint` values were honored, authenticated `/v1/state` reads succeeded over the Unix control socket, wrong and missing bearer tokens both returned `401`, and a created server survived a restart from `state.store/`.
- 2026-04-21: `pkg/network` now serves peer traffic over a direct TCP transport with length-delimited protobuf-wire frames, so peer-facing operations no longer depend on `/_aether/p2p` HTTP/JSON even though `pkg/node.Service` still marshals its operation payloads internally through the existing request structs.
- 2026-04-21: `pkg/storage` now persists runtime state into `state.db` via a SQLite-backed store with SQLCipher-style key/open hooks at the storage seam, and first-run opens migrate the old AES-GCM `state.store/` directory into the SQL backend before archiving the legacy directory.
- 2026-04-21: Replaced the plain `mattn/go-sqlite3` placeholder with the real `github.com/mutecomm/go-sqlcipher/v4` driver and moved keying into the SQLCipher DSN (`_pragma_key=x'...'`) so the database is encrypted before the first query rather than keyed after open.
- 2026-04-21: Review found that several user-facing state writes in `pkg/node/service.go` still mutate memory before `saveLocked()` and do not roll back on failure (`CreateServer`, `CreateDM`, `sendMessage`, `EditMessage`, `DeleteMessage`, `MarkNotificationsReadScoped`, identity rebinding), so the service can return an error while leaving live state ahead of durable storage.
- 2026-04-21: `cmd/aether/main.go` config merging still treats default-valued flags as absent, which keeps file-config precedence workable for most fields but leaves the explicit-default override edge case unpinned.
- 2026-04-21: The SQLCipher-backed store now keeps only reopen-critical salt/key-check material in `state.db.meta.json`; that lets `pkg/storage` derive the DB key before `sql.Open`, while schema version and bucket contents stay inside the encrypted database and legacy `state.store/` migration behavior remains intact.
- 2026-04-21: Final live QA on `bin/aether` passed the startup guardrail: `--mode definitely-not-a-mode` exited 2 with `invalid --mode "definitely-not-a-mode"; expected client|relay|bootstrap|archivist`, while a valid client startup printed `xorein runtime ready ... control=/tmp/tmp.scwTYk00pt/data/xorein-control.sock`.
- 2026-04-21: Live control-plane checks on the Unix socket succeeded locally: `GET /v1/state` returned the snapshot with `control_endpoint`, `identity`, `servers`, and `peer_id`; wrong bearer tokens returned `401` with `{"code":"unauthorized","message":"missing or invalid control token"}`; `GET /v1/events` emitted `event: ready` / `data: {"version":"v1"}` and `curl --max-time 2` exited 28 as expected for the open SSE stream.
- 2026-04-21: Restart recovery verified by creating `POST /v1/servers {"name":"qa-server","description":"restart persistence check"}` which returned `server-389f3d3eede6ca0d`; after relaunching from the same data dir with `nohup`, `GET /v1/state` still showed exactly one server named `qa-server` with the same ID and the same `peer_id` (`ae7fff6dcf2f6e5ac2712da6218d0083`).
- 2026-04-21: Live QA rerun on `/tmp/xorein-qa.5yPAPd` confirmed the current checkout still starts cleanly, rejects a wrong bearer token on `/v1/state`, emits the initial `ready` SSE event, preserves `qa-server` across a restart from the same data dir, and fails fast on `--mode definitely-not-a-mode` with exit code 2.
- 2026-04-21: The `pkg/node` notification-summary tests do not need live peer transport to validate aggregation behavior; a test-only stub `network.Runtime` plus signed synthetic `applyDelivery()` messages exercises the real notification/control-plane code while cutting the focused summary suite from multi-second live-network startup to sub-second local execution.
- 2026-04-21: The control-only test helper in `pkg/node/service_test.go` stays safely test-local: it injects a no-op `network.Runtime` only for `Service.Start()`, while synthetic signed deliveries still flow through `applyDelivery()` so summary assertions remain on real aggregation logic instead of transport startup.
- 2026-04-21: Re-reviewed the summary-test narrowing in `pkg/node/service_test.go`; `startControlOnlyService()` stays test-only, `applyRemoteMessageAt()` still feeds signed synthetic deliveries into `host.applyDelivery()`, and the control API assertions continue to exercise real notification aggregation without changing product behavior.
