# v1.2 Execution Artifacts

## Status
Implementation artifacts for v1.2 (`F12`) and specification artifacts for v1.3 (`F13`).

## Layout
- `phase0/`: scope lock, traceability matrix, gate ownership.
- `phase1/`: backup format and encryption profile.
- `phase2/`: additive proto changelog and UX contract.
- `phase3/`: Podman recovery scenario baseline.
- `phase4/`: v1.3 Spaces/chat specification package.
- `phase5/`: evidence bundle, risk closure, conformance, sign-off, evidence index.

## Planned vs implemented
- v1.2 runtime and test artifacts are implemented in code paths referenced by phase docs.
- v1.3 documents in `phase4/` remain specification-only and do not claim implementation.
