# Phase 5 As-Built Conformance

- `pkg/v13` packages implement all `F13` requirements from v12 spec inputs.
- Deterministic UI contract (`docs/v1.3/phase2/p2-ux-contract.md`) ties to code in `pkg/v13/ui`.
- Podman manifest + `relaypolicy` guard ensures relay no-data boundary remains intact.
