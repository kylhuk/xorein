# P2-Integrity Reason Taxonomy

This artifact catalogs deterministic failure reasons exposed by the v22 history integrity layer. It lists `HISTORY_HEAD_INVALID_SIGNATURE`, `MANIFEST_HASH_MISMATCH`, and `SEGMENT_NOT_FOUND` so clients can react uniformly and operators can audit verification issues.
