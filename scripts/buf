#!/bin/bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BIN_DIR="$ROOT_DIR/.cache/xorein-tools/bin"
if [[ ! -x "$BIN_DIR/buf" ]]; then
	mkdir -p "$BIN_DIR"
	GOBIN="$BIN_DIR" go install github.com/bufbuild/buf/cmd/buf@v1.39.0
fi

exec "$BIN_DIR/buf" "$@"
